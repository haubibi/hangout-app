import styled from "styled-components";
import { Upload } from "antd";

export const FormImagesUploadContainer = styled(Upload)`
    display: flex;
    width: 100%;
    flex-direction: row;
    flex-wrap: nowrap;
    justify-content: flex-start;
`