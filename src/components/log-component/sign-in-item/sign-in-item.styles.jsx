import styled from "styled-components";
import { Link } from 'react-router-dom';
import {
    Modal
} from 'antd';
export const SignInItemContainer = styled.div`
    width: 100%;
    height: 100%;
    max-width: 600px;
`;
export const ForgetPasswordLink = styled(Link)`
    color: black;
`;

export const EmailVerifyModal = styled(Modal)`
    
`;

export const EmailVerifySpan = styled.span`
    
`;